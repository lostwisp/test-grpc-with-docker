services:
  twodocker:
    build:
      context: Two
      dockerfile: Dockerfile
    network_mode: host
    ports:
      - 50051:50051

  onedocker:
    build:
      context: One
      dockerfile: Dockerfile
    network_mode: host
    ports:
      - 50051:50051
